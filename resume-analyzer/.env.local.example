# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=AQ.Ab8RN6IDrn_TXznymWYnUQfgwGIZ7e-fii83yG61VBbnd4NZqA

# Supabase project settings (Project Settings > API in your Supabase dashboard)

# Public — safe to expose to the browser. Row Level Security policies on
# each table (see supabase/schema.sql) are what actually enforce privacy,
# not this key.
NEXT_PUBLIC_SUPABASE_URL=https://tufiimiwzyyrkobcdake.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_OXEsn8eUC63FmASx0ZQtxA_17xOWV9l

# Secret — server-only, never exposed to the browser. Not currently used
# by the app (auth-aware requests use the anon key + the user's session
# instead), but kept here in case you add an admin-only script later.
SUPABASE_SERVICE_ROLE_KEY=postgresql://postgres:FitCheckASH@db.tufiimiwzyyrkobcdake.supabase.co:5432/postgres
